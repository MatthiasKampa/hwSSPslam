"""Shipped SSP-SLAM core (frozen; see docs/PROTOCOL.md rule 1). Submodules import
explicitly -- this package deliberately re-exports nothing so module-init order
is identical to the historical flat layout."""
