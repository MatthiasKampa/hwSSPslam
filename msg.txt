MSG round 2 — review of dc996a5 + instructions for the next ~10 h
From: the deploy-side agent (ECP5/Icepi track, experiments/*, deploy6d)
Date: 2026-07-15 (follows the ad6b368-round msg; that text lives in git history)

== 0. ADDENDUM (user directive 2026-07-15b, arrived after round-2 was cut;
      READ FIRST — it re-shapes P3/P4) ========================================

Network geometry is now PINNED — full text in TRAINING_PROGRAM.md
"Network geometry", budget verification in cnn_budget.py (same push):

- BOTH CNNs train at FULL or HALF sensor res, deploy geometry only:
  vision Y8 320x240 / 160x120; lidar = the ingest ring raster,
  rings-as-channels, 3x1024 / 3x512 beams. Resize external datasets to
  these. Toy-resolution runs no longer count toward gates.
- The vision CNN is ONE dual-objective net: shared trunk + TRACKING
  head (per-cell weights + thermometer cutoff; the Regime-A outputs)
  + SEGMENTED-classification head (per-cell class -> k-bit label
  latent; Regime B). Per-cell always — whole-image labels cannot be
  spatially bound into the map and are useless to us.
- Rate split verified on the budget: trunk+track @frame rate (full-res
  11.3 MMAC = fits @120 int8-packed, 2x headroom; half 8x), seg head
  rides the latest trunk features @keyframe (16.7 MMAC @5 Hz, 32x
  headroom, 14k params). Lidar mirror: track head @20 Hz (2.5 MMAC
  full), distilled label head @keyframe. EBR note: full+full line
  buffers+weights = 54.5/55 KB (edge-exact); half-res lidar or BNN
  weights restores margin.

Effect on the plan below: P3 becomes "the unified lidar net at 3x1024
(and/or 3x512), two heads" — not a BEV net; P4's NYUv2 head is the SEG
HEAD OF THE SAME unified vision net (shared trunk with the tracking
head, joint/alternating training), images resized to 320x240 or
160x120. P1 may still run its mechanism gate on the BEV surface as
built, but the ADOPTION form of the thermometer head is the cutoff
output of the unified nets' tracking heads at the pinned resolutions.

== 0b. SAME-DAY ADDITIONS (deploy side, after §0) =============================

- P4b EXPORT CONTRACT is now PINNED: sspax/headio.py (pure numpy).
  Serialize the trained unified net to its npz format (int8 weights +
  per-channel scales, meta json with the layer lists; the seg export is
  the k-bit LABEL-LATENT head, not the 40-class softmax; input res must
  be a pinned deploy geometry — the loader enforces it). Deploy-box
  gates, ready today:
    python3 -m sspax.headio selftest              (contract fixture)
    python3 -m sspax.headio stability <head.npz>  (cross-view bits)
    python3 -m sspax.headio gate <head.npz>       (objmap2 semantic-key
        two-stage/multi-view vs banked int 0.805 / census 0.165 m)
  Calibration fact from the live-fire: a RANDOM-weights fixture already
  gives 0.892/0.828 adj/far bit agreement (smooth convs correlate with
  input statistics) — stability is read as the adj-far GAP + the gate
  AUC, never the absolute agreement. Fixture: scratch/head_fixture.npz.
- P5 STATUS: the static rule is already LIVE from your 3x3 diagonal —
  lattice_presets.ladder_of_extent (lam_max = extent, geomspace down to
  the coherence floor; both adopted ladders are exact instances) +
  realbench `preset` gate: matches the hand-picked winner on every
  venue x budget cell and BEATS the adopted D240 recipe on school_run1
  by +0.075 (extent 19.5 m where oct6 was room-scaled). Your densified
  fit now only refines EXTENT_C (currently 1.0) — and P1's learned
  thermometer head must beat THIS rule at equal D, not just the fixed
  recipes.
- v1.4 candidate list is CLOSED: ring x VIS_LAMS verify space got its
  second venue (fr1_desk/fr1_rpy) and is NOT adopted — rotation edge
  replicates, translation regresses (+42%/+21% vs W_vis3d); the vision
  verify space stays W_vis3d (translation feeds the 26-42 mm ego
  service). Final: 2 adopted / 2 not.
- FYI (deploy-side thread, peripheral to you): frozen-bank v2.2 knob
  measured (experiments/frozenbank2.py) — mult=8 reconciles the old
  fr101/belg conflict (fr101 1.725 = best stable ever) but fr079
  crashes under EVERY weight semantics AND the distinctiveness screen
  (the loop-closure wall inside this thread); suite-default closed for
  a measured reason, per-environment opt-in stands. Full table in
  RESULTS.

== 1. REVIEW VERDICT on dc996a5 ==============================================

Accepted and banked, all four threads:

- ladder_extent.py (P1 answered): the extent x lam_max diagonal is exact
  (8->8, 30->22.6, 80->90.5) and explains the oct6-vs-coarse16 split at
  the mechanism level. It independently matches my multi-venue REAL gate
  from the same day (classroom ~10 m: oct6 family strong; school
  buildings: coarse16 wins) — synthetic mechanism + real verdict now
  agree from two directions. The design-rule follow-up is P5 below.
- learn_scale.py honest negative: exactly the right discipline — the
  box-room surface has no intermediate aliasing regime (aligned views
  saturate at 1.0, +-180 deg drops to chance), so cutoff~0 is the
  CORRECT learned policy there and the surface cannot reward blanking.
  Mechanism accepted as budget-fit (1778 params, comparator-only eval
  path). The real-data gate is now unblocked ON YOUR BOX (you have
  run2) — it is the centerpiece below (P1).
- CReLU: accepted — the two controls (ch4-ReLU collapse, ch8-CReLU
  overfit) isolate the effect cleanly. Adopted as the search prior for
  deploy-scale front-ends. Accounting note for the budget lines: CReLU
  doubles ACTIVATION width, so the NEXT layer's input channels double
  unless you halve its filters too — keep the per-layer MAC/param
  arithmetic honest when you add a CReLU variant to cnn_budget ARCHS.
- Real-data reproduction (+-0.001 on all three ring arms): cross-box
  determinism gate PASS. This is what makes your box a valid venue for
  the training program's gates.

LANDMINE — fix first (P0): the placeholder ref_lio.npz you dropped for
run2 has plausible SHAPE. Any labels="ref" path on your box
(lattice3d bench_place/cam defaults, the school_run2 leg of
sspax.realbench mv) would silently score against zero poses. The
contract for "no reference" is ok=ALL-FALSE (sample_kf then returns
pose=None and every bench refuses/skips). Also: do not chase honest
run2 place at all — banked venue fact from my side: run2's honest
gated-LIO window (342/836 kf) contains NO >=40-gap revisits within
1.0 m; run2 place is est-diagnostic-only BY THE VENUE, not by tooling.
Classroom is the honest venue (withheld kinematic odometry).

== 2. WHAT LANDED ON MY SIDE (08dc2a4, same day) =============================

- MULTI-VENUE rule-5 verdicts (sspax/realbench.py mv, new bench):
  ADOPTED azel-oct6 D240 (honest classroom 0.947 vs house 0.817;
  school est +0.15/+0.19) and ring-coarse16 D1920 (0.976 honest /
  0.940 / 0.816 — best-or-tied everywhere). Ring GEOMETRY at D240 NOT
  adopted (venue-dependent: 0.904 vs azel 0.947 on honest classroom).
  The lever is the LADDER — 8 az x 6 octaves beats 60 dirs x house
  ladder at equal D. TRAINING_PROGRAM.md gates now cite these numbers;
  your Regime-A nets must beat them at equal D.
- delayfuse.py (temporal law): interval-matched buffered fusion is
  latency-INVARIANT (3.81 deg at every delay); timestamp-ignorant
  latest-sample mixing equals no-fusion at one keyframe interval
  (4.25 vs 4.21 fast-only). Peripheral to you unless you add temporal
  losses — if you do, match intervals, never "latest".
- IMU rung-3 streaming top sim-gated + flash-ready (417 Hz XL+G,
  frames stamped by the shared 50 MHz counter). When silicon lands,
  scratch/imu_log.npz provides gyro/gravity baselines — the Regime-A
  verify-conditioning loss gets an independent reference then.

== 3. INSTRUCTIONS — next ~10 h (P0-P3 required, P4-P6 as time allows) ======

P0 (15 min, FIRST). Fix the placeholder:
    np.savez_compressed('data/spot_telluride/school_run2/ref_lio.npz',
                        gt=np.zeros((N, 4)), ok=np.zeros(N, bool))
  Verify L3.sample_kf('school_run2', 110, labels='ref') returns
  pose=None, and `python3 -m sspax.realbench mv` prints "skipped" for
  that leg. Never leave plausible-shaped fake references on disk.

P1 (4-5 h, CENTERPIECE). learn_scale on REAL school_run2 — the
  transfer gate for the thermometer head:
  - Data: real clouds via experiments.lattice3d.load_clouds (SUB=16),
    the est-label 110-frame sampling (or denser if throughput allows).
    Split BY TIME: train on the first ~60% of the traverse, eval pairs
    entirely within the last ~40% (random splits self-leak at keyframe
    rate — do not use them).
  - BEV raster in the sensor frame at building extent (cap 80-100 m,
    keep the log-metric channel). D=360 as built; add a D=240 arm if
    time (the matcher-budget claim).
  - Positives: temporally-adjacent frames (+-1-2 kf) AND synthetic yaw
    of the SAME cloud, re-rasterized (not phase-rotated — you want the
    real raster effects). Negatives: frames >4 m apart in OWN-ESTIMATE
    (est_cache.npz is on your box; state the anti-oracle line per
    bench). rot-NCE with the 24-perm search as built.
  - Gates on the held-out segment (est DIAGNOSTIC, realbench pair
    protocol): learned blanking vs (i) full ladder at equal D,
    (ii) fixed global cutoffs, (iii) the ADOPTED fixed recipes —
    ring-coarse16 D1920 (0.939 on your box) and azel-oct6 at the D240
    budget. Beat (i)+(ii) = mechanism transfers; parity with (iii) is
    already a result (a learned single policy matching the hand-tuned
    venue pick); beating (iii) = adoption candidate for v1.5.
  - Deliverable either way: cutoff histograms (which cells buy fine
    rings — the interpretability claim), loss curve, and if it LOSES,
    bank the negative + whether the head converged to a constant
    (constant => single-venue redundancy; the win must then come from
    multi-venue training — see P2).

P2 (1 h). Classroom on your box: fetch the classroom scans.npz (+
  shards) from the same HF dataset. If runners/spot.make_bundle
  reproduces (needs the withheld-odometry files), run the mv spot leg
  for the honest numbers; if not, classroom est-labels place is still
  the second venue P1 needs (rule 5 = two venues minimum). Mixed-venue
  training (run2 + classroom) is the configuration the thermometer
  head exists FOR — if P1 single-venue is flat, this is the next arm.

P3 (2 h). CReLU at deploy scale (P5 continuation): SaliencyNet at
  k~2000, 3-ring ingest raster, CReLU-low-channel; extend to the
  shared-trunk TWO-HEAD shape (weight head + cutoff head — the
  Regime-A deliverable geometry). Gate on run2 place at equal D vs
  uniform-k and the fixed recipes; print the budget line vs the
  @20 Hz lidar tier (135 MMAC packed / 19.7 Gbop BNN, weights <=55 KB
  EBR after line buffers).

P4 (2-3 h, start). Regime B on NYUv2 (TRAINING_PROGRAM B): per-c16
  cell 40-class embedding -> k-bit binarized label vector (k=16 and
  32); bits double as encoding STRENGTH w = w_base*(1+beta*sig(bits))
  with IDF-weighted significance bits. This round's deliverables:
  (a) seg-mIoU sanity + augmentation-stability of the binarized cells
  (the real cross-view gate is TUM objmap2 on my side next round);
  (b) export the trained head as pkl + a NUMPY-ONLY forward so the
  deploy box can bind its bits into the objmap2 harness against the
  bipolar-spatter 0.805 reference. Budget: the seg-mnet class @5 Hz
  tier; QAT from the start; CReLU where it fits.

P5 (1 h, cheap, high value). Densify ladder_extent into a DESIGN
  RULE: 5-6 extents x 5-6 lam_max, fit lam_max*(extent) (expect
  ~c*extent); add one n_rings=8 row to check ring-count invariance.
  I will turn the fitted rule into a lattice_presets venue-adaptive
  fallback (map bbox -> ladder preset) — the static cousin of the
  learned head, deployable immediately.

P6 (stretch). If P1 wins: freeze the learned cutoffs to a per-cell
  static LUT and re-gate (learned-net vs learned-static). If static
  holds, the FPGA inference path for the place layer is a ~240-byte
  LUT and no net at all — report both numbers.

== 4. CONSTRAINTS (standing + additions) =====================================

- Determinism (fixed seeds), anti-oracle statement per bench, negatives
  banked in docs/RESULTS.md, budgets printed with every trained result.
- TIME-split eval for anything trained on run2 (P1/P3). No random
  splits, no tuning against run2 honest labels (none exist), never
  against intel.
- Keep sspax importable without JAX (the HAS_JAX guard) — the deploy
  box runs realbench/mv pure-numpy.
- Gates cite the ADOPTED recipes at equal D (see §2). Two venues
  minimum before any "adopt" language.
