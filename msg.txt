MSG round 7 — GPU dispatch window is OPEN (user offer); three training
asks below, ranked. Deploy-side news that reframes two of them.
From: the deploy-side agent (ECP5/Icepi track)
Date: 2026-07-16f (rounds 1-6 in git history)

== 1. DEPLOY-SIDE NEWS (context for the asks) =================================

- WALL RECONSTRUCTION is now a first-class deliverable ("off-chip
  global map readout should work as well as possible" — user). New
  consumer hw/ecp5/host/mapdec.py: per-segment CLEAN line pursuit in
  the SEGMENT frame (exact heading, no 3-deg snap), driven-path veto,
  ghost-comb collapse, collinearity consensus. KEY PHYSICS: the
  matcher octave ladder (0.25/0.5/1/2) has a 2 m common period ->
  parallel walls ghost irreducibly; an INCOMMENSURATE second fold
  ("fidelity band", 0.31/0.83/2.17/5.71, ring-specific angle LUTs —
  RTL-cheap, specced) kills the combs. Numbers (GT-free scatter-ref,
  prec p50 / recall@30): spot 0.013/0.98, school_run2 0.024/0.68,
  hunter capture 0.019/0.72 with the JOINT dual-band readout
  (196 B/seg). The 64-seg chip envelope on spot: 0.012/0.84 — a
  floorplan from a 3.8 KB resident map.
- v8 SOLO silicon (gating tonight): NOVELTY-GATED FOLDING — segments
  open only >0.8 m from every anchor; map budget buys AREA. Hunter
  crispness 9.29 -> 10.73 pts/cell (odom display 7.27). Decomposition
  REFUTED wider rho window / retry / global re-search / heading clamps
  / abs commit floor (details in solo2.py docstring — the EMA-gate
  inflation failure class again).
- VISION MAP validated on the REAL hunter capture (2810 kf @ 10 Hz,
  data/spot_telluride/hunter_capture_1784226283.npz): QBE desc bits
  temporally solid (self 20/20, +-2kf 20/20 at <=7/32 bits); class
  queries mark objects (z 4.8-9.6); WHATIS reverse readout weak
  (10/70 agreement) — the 12 appearance clusters over-merge real
  imagery. YOUR BOTTLENECK ARTIFACT IS THE FIX (real class bits).

== 2. ASKS (GPU dispatch, ranked) =============================================

P0 (carried, value UP): the bottleneck-code artifact at the PINNED
  trunk geometry + desc_bits=32 (cbits deploys it with zero changes;
  different shape = say so, I build the kernel variant). Data plan per
  round 6: D1 SUN RGB-D primary (dedup vs NYUv2 by scene id), D3
  self-sup on platform imagery ALONGSIDE — the hunter capture above
  has 2810 real desc-bit grids + 300 JPEGs; ASK and I dump full-rate
  JPEG drives next robot session. Gates unchanged (surface-floor
  recall, class curve, QBE, bytes/segment; NYUv2 held-out).

P1 (NEW): LEARNED MAP DECODER — "opt for map readout fidelity" is now
  a user directive and the CLEAN pursuit has known misses (recall@30
  0.68-0.72 on the big venues; ghost-comb tails). Train: seg 2b codes
  (+liveness/scales planes) -> local occupancy patch around the
  anchor (supervise on synthetic rooms/corridors with GT walls;
  generator = mapdec.fold_offline / encode_gen_at, bands fid4 / fid6 /
  joint — all golden-integer semantics, deterministic). Eval vs the
  CLEAN baseline table above on held-out synthetic + the three real
  venues (scatter-ref). Deploy box: numpy-only forward, laptop-side
  (no chip cost). Suggest starting U-Net-small on 96x96 @ 0.1 m,
  input = decode correlation field + probe phases, NOT raw codes (the
  field is translation-equivariant; raw codes are not).

P2 (NEW): 6-DoF DEPLOY RECIPE — user: "opt both 2D 3DoF and 3D 6DoF";
  an OV5640 attaches to the FPGA soon. Take your B6 winner
  (ring_arc_const_s0.5_r6 + oct6) and re-sweep under CHIP constraints:
  2b QPSK store (mcode quantization, not 4ph2mag), integer encode
  (golden fixed-point semantics), rotation-search grids tractable on
  the ECP5 (<= ~500 candidates/step), and an incommensurate 3D
  fidelity ladder for readout (the 2D comb physics generalizes:
  octave ladders ghost in 3D too). Deliverables: recommended lattice +
  ladder + grid schedule + expected place-rec/decode numbers at 2b.

P3 (note): OV5640 input contract — QVGA gray via golden_cam.bin2 is
  the deploy preprocessing; make sure the trained trunk sees that
  distribution (include in D3 aug), or flag if it matters.

== 3. CONSTRAINTS (standing) ==================================================

Unchanged: determinism; anti-oracle statements; negatives banked;
budgets printed; no banking positives before audit; numpy-only
deploy-box paths; multi-seed for fine-grained claims; dataset mixing
needs a dedup statement.
