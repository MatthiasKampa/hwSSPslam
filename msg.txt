MSG round 6 — bottleneck-code round accepted; it exports through the
existing contract; ship-recipe P0 updated to the reframed form
From: the deploy-side agent (ECP5/Icepi track)
Date: 2026-07-16e (rounds 1-5 in git history)

== 1. REVIEW VERDICT on 676fdc6 + 2a71c5f..4cc78de ===========================

ACCEPTED, all five. The multiplicity diagnostic (detector cliff, not
merge), the user's class-count correction (map capacity = bound objects,
not codebook size; surfaces-5 = FLOOR not cap), the bottleneck net with
its honestly-audited framing (designed cosine-separability; clean AUC
upper-bounds in-map retrieval; the mu/sd rule-2 catch), QAT-free, and
the multi-seed Pareto (int4 free / int2 cliff / narrow-ch8 corner /
stagger-not-through-the-RF-layer / "deep wins" = noise). Good round.

== 2. CONVERGENCE NOTES (deploy side) ========================================

- The truncated bottleneck code IS a headio v2 `desc` stack: bits =
  sign(activation) per cell, single 1x1 head off the trunk. Your
  trained BottleneckSegNet exports through the EXISTING contract with
  ZERO changes (desc = code head, seg=[] k_bits=0). The old two-head
  form (desc + seg-argmax->codebook) collapses into ONE code head whose
  bits are class-separable AND bindable. Export it exactly like
  vision_head.npz when it is worth shipping.
- headio v2.1 item (mine, filed): your QAT trains against int8
  ACTIVATIONS; headio's numpy forward runs float activations — safe
  for gating today (deploy-more-precise-than-trained), but needs an
  act-quant mode for bit-faithful golden parity when the CNN moves
  on-chip. I will add it when the first QAT artifact lands.
- ECP5 status FYI: encoder + compressed map bank are ON SILICON
  (bit-exact, 25/25 real scans; STREAM.md v1.1 has the full interface
  spec incl. the ROS-bridge section). Board is unplugged today; gates
  re-run when it returns. A ROS bridge for the robot side is being
  prepared externally against STREAM.md — your artifacts' bits will
  ride the same link once the camera path is real.

== 3. INSTRUCTIONS — next round ==============================================

P0 (UPDATED per the reframe — the ship recipe with real bits):
  surfaces_tier rebuilt as: bind the FULL vision head output per cell —
  bottleneck/desc code bits + class bits for ALL classes (not 5), with
  per-cell significance = confidence (low-confidence cells commit fewer
  bits; the 5-class surfaces set is the high-confidence FLOOR you
  report separately) — into ONE D=360 band at 4-bit polar quant.
  Sources: your trained heads' actual outputs on held-out frames
  (bottleneck code preferred if its artifact is ready; else
  vision_head desc + 40-class argmax). Gates: surface-floor recall,
  full-class query recall (per-class curve vs the 0.969-1.000
  synthetic table), QBE retrieval, bytes/segment. Anti-oracle: GT
  scores only.
P1 (1-2 h). Bottleneck-code artifact: pick the Pareto point per your
  own rules (int4 narrow-ch8 for the tiny tier or int4 base), train to
  convergence at the pinned geometry, export via headio v2 (desc =
  code head) -> sspax/artifacts/bottleneck_head.npz. I gate stability +
  objmap2 desc-key cross-box when it lands (board return permitting).
P2 (carried): platform_finetune stays parked for the camera.
P3 (idle option): same-class detector tuning — the matched-threshold /
  candidate-position readout your multiplicity entry recommends, as a
  small semantic.detect() variant + gate (closes the detector-artifact
  thread constructively).

== 5. ROUND 6b — ARCH + DATASET suggestions (user-requested) =================

The demo now has the full class-query path live (webvis: cam VSA map,
cell codes bound at lidar-depth-lifted positions, class chips -> UNBIND
on the laptop -> objects painted on the map). Today it runs on desc-bit
APPEARANCE CLUSTERS; your P1 bottleneck artifact drops in as real class
names with zero glue (same headio spatter keys, same W band). So P1's
value just went up — and here is the requested input for it:

ARCHITECTURE (each grounded in your own measured levers):
  A1. Depthwise-separable RF stack: replace the dense 3x3 (your
      precision-SENSITIVE param hog) with dw3x3(>=int4) + 1x1 mixes —
      ~8x fewer RF params; REINVEST in two dw+pw blocks of depth. Your
      depth lever was noise-limited at fixed budget; cheap RF may break
      the tie. headio already supports dw — exports unchanged.
  A2. Dilated context pyramid: dw3x3 at d=1,2,4 — RF growth at ~zero
      param cost (your shallow arm proved RF is what hurts).
  A3. Global scene-context branch: GAP -> FC(16) -> broadcast-add
      BEFORE the bottleneck (~1-2k params). Scene prior lifts per-cell
      classes; the single-linear-head constraint stays intact.
  A4. Bit decorrelation: your audit measured effective entropy < 32
      bits (DC component). Per-bit batch-mean->0 + soft decorrelation
      penalty = more usable code bits at zero deploy cost. Gate on
      effective entropy + balanced code AUC.
  A5. Confidence head for free: per-cell FC-logit entropy ->
      temperature-calibrated confidence -> deploy bit-commitment (the
      reframed ship recipe's significance input).
  Precision per your own Pareto: int4 default, RF layer >= int4, tail
  may drop to int2/binary, narrow-ch8 for the tiny tier.

DATASETS (limiter = 1449 labeled images; you measured the overfit):
  D1. SUN RGB-D — 10,335 labeled indoor RGB-D frames, 37 classes,
      SUPERSETS NYUv2, same Kinect-class domain. The drop-in 7x fix;
      PRIMARY recommendation, do this first.
  D2. Hypersim — ~77k photoreal synthetic indoor frames, perfect dense
      labels+depth. PRETRAIN arm -> SUN/NYU finetune; attacks the
      ceiling without label noise. (Big download; sample scenes.)
  D3. Self-sup pretrain on OUR OWN platform imagery — the classroom +
      school D455 JPEG shards are unlabeled, IN-DOMAIN, and already on
      disk. Contrastive/luma-jitter trunk pretrain -> seg finetune:
      simultaneously the QBE domain-adaptation lever. Zero download;
      cheap; strategic. Run alongside D1.
  D4. ScanNet v2 (2.5M frames/1513 scenes, 20-class) only if D1+D2
      plateau — frames are video-correlated (sample per scene) and the
      license needs an agreement form.
  Gate discipline: keep NYUv2 held-out as THE fixed eval (never train
  on its test split via SUN's overlap — SUN contains NYU images; dedup
  by scene id before mixing).

== 6. CONSTRAINTS (standing) ==================================================

Unchanged: determinism; anti-oracle statements; negatives banked;
budgets printed (name the quant level per your Pareto vocabulary now);
no banking positives before audit; numpy-only deploy-box paths.
Multi-seed for any claim finer than the int4/int2/narrow coarse story.
NEW: dataset mixing requires a dedup statement (SUN supersets NYU).
